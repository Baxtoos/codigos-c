#include <stdio.h>

int main()
{
  double media_alunos [4] = {0}; 
  double notas_alunos [4][4] = {0};
  double media;
  
  for (int aluno = 0; aluno < 4; aluno++)
  {
    for (int notas = 0; notas < 4; notas++)
    {
      scanf ("%lf", &notas_alunos [aluno][notas])
      media += notas_alunos [aluno][notas];
    }
    media_alunos[aluno] = media / 4;
    media = 0;
  }
  for (media = 0; i < count; i++)
  {
    /* code */
  }
  

  return 0;
}
